# The tests regenerate reports and compare them byte for byte, so the line
# endings must not depend on the platform the clone happens on.
* text=auto eol=lf

*.png  binary
*.gz   binary
*.ps1  text eol=crlf
